<script>
    function SubmitFrm() {
        var Searchtxt = document.getElementById("txtSearch").value;
        if (Searchtxt.trim().length > 0)
            window.location = "Arizali-Cihaz-Takip?TicketId=" + Searchtxt;
    }
</script>
 
<nav class="header-top navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#header-top-navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        @*<a class="navbar-brand" href="@Url.Content("~/ana-sayfa")">Hoş geldiniz</a>*@
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="header-top-navbar-collapse">
        <ul class="nav navbar-nav"></ul>

        <ul class="nav navbar-nav navbar-right">
            @*@Html.Partial("/Views/Shared/bootstrap/Header/_ChangeLayout.cshtml")
                @Html.Partial("/Views/Shared/bootstrap/Header/_ChangeTheme.cshtml")*@

            @*<li> 
                <div class="search-panel">
                    <input name="txtSearch" type="text" id="txtSearch" class="field" placeholder="Servis Kayıt Numarası" />
                    <input type="submit" name="btnSearch" value="Kayıt Ara" id="btnSearch" class="btn" onclick="javascript:SubmitFrm()" />
                </div>
            </li>*@
            <li>
                <form class="navbar-form" role="search">
                    <div class="input-group">
                        <input type="text" class="form-control" name="txtSearch" id="txtSearch" placeholder="Servis Kayıt Numarası">
                        <div class="input-group-btn" title="Servis kaydı aramak için tıklayınız...">
                            <button type="button" onclick="javascript:SubmitFrm()" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
                        </div>
                    </div>
                </form>

                    @*<form class="navbar-form" role="search">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Servis Kayıt Numarası" name="srch-term" id="srch-term">
                    <div class="input-group-btn">
                        <button class="btn btn-default" type="submit"><span class="glyphicon glyphicon-search"></span></button>
                    </div>
                </div>
            </form>*@
</li>

            @if (User.Identity.IsAuthenticated)
            {
                <li><a href="@Url.Content("~/dashboard")" >  Hoşgeldin @User.Identity.Name </a></li>
            }
            else
            {
                 <li><a href="@Url.Content("~/dashboard")"> <span class="glyphicon glyphicon-log-in"></span> Giriş </a></li>
                @*string displayName = "";
                var userId = 12; // SecurityHelper.ActualUserIdOrNull;
                if (userId != null)
                {
                    displayName = "Ömer KAPLAN";
                    //displayName = Surat.Web.Security.UserCache.ById(userId.Value).DisplayName;
                    //Hoşgeldin @(displayName.Length > 28 ? (displayName.Substring(0, 25) + "...") : displayName)

                     if (SecurityHelper.HasRight(new AccessRight("CmsPageEditor")))
                        {
                            <li><a id="left-panel-link" href="#left-panel">Tema & Eklenti Yönetimi</a></li>
                        } 
                    <li><a href="@Url.Content("~/uye")"><span class=""></span>Bilgilerim (@displayName)</a></li>
                    <li><a href="@Url.Content("~/uye/siparislerim")"><span class=""></span>Sipariş Takibi</a></li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Listelerim <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="@Url.Content("~/favorilerim")">Favori Ürünlerim</a></li>
                        </ul>
                    </li>
                    <li><a href="@Url.Content("~/cikis")"><span class=""></span> Çıkış</a></li>
                }*@
            }

        </ul>
    </div><!-- /.navbar-collapse -->
</nav>
